
@{
    ViewBag.Title = "All Orders";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
    List <Klickit_Task.Models.Order> orders = ViewBag.Order;
}

<div class="content-wrapper">
    <!-- Content -->

    <div class="container-xxl flex-grow-1 container-p-y">
        <h4 class="fw-bold py-3 mb-4"><span class="text-muted fw-light"> Orders /</span>All Orders Details</h4>
        <!-- Basic Bootstrap Table -->
        <div class="card">
            <h5 class="card-header">Orders Details Table</h5>
            <div class="table-responsive text-nowrap">
                <table class="table table-hover">
                    <thead>
                        <tr>
                            <th>User Email</th>
                            <th>Date</th>
                            <th>Time</th>
                            <th>Total</th>
                            <th>Status</th>
                        </tr>
                    </thead>
                    <tbody class="table-border-bottom-0">
                        @for (int i = 0; i < orders.Count; i++)
                        {
                        <tr>
                            <td><strong>@orders[i].User.User_Email</strong></td>
                            <td>@orders[i].Date</td>
                            <td>@orders[i].Time</td>
                            <td><strong>@orders[i].Total</strong></td>
                            @if (orders[i].Status == "Pending")
                            {
                                <td><span class="badge bg-label-warning me-1">Pending</span></td>

                            }
                            @if (orders[i].Status == "Accept")
                            {
                                <td><span class="badge bg-label-success me-1">Accept</span></td>

                            }
                            @if (orders[i].Status == "Reject")
                            {
                                <td><span class="badge bg-label-danger">Reject</span></td>

                            }
                        </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

